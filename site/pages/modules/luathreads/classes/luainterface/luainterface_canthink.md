<function name="CanThink" parent="LuaInterface" type="classfunc">
	<description>
		Returns whether this interface currently has its own dedicated thread running. (See <page>LuaInterface:EnableThinking</page>)
	</description>
	<realm>Server</realm>
	<rets>
		<ret name="hasThread" type="boolean">Returns `true` if the interface has its own thread</ret>
	</rets>
</function>