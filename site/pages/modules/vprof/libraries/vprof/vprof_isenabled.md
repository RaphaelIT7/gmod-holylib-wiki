<function name="IsEnabled" parent="vprof" type="libraryfunc">
	<description>
		Returns `true` if vprof is enabled/running.
	</description>
	<realm>Server</realm>
	<rets>
		<ret name="running" type="boolean">`true` if it's running</ret>
	</rets>
</function>