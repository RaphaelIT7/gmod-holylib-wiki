<function name="CreateCopy" parent="EntityList" type="classfunc">
	<description>
		Returns a new EntityList that contains the exact same values as this list (This is way faster than manually creating a copy)<br>
		<added version="0.8"></added>
	</description>
	<realm>Server</realm>
	<rets>
		<ret name="newlist" type="EntityList">Returns the new EntityList</ret>
	</rets>
</function>