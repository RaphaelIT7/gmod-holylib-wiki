<function name="IsLocked" parent="stringtable" type="libraryfunc">
	<description>
		Returns whether all stringtables are currently locked.
	</description>
	<realm>Server</realm>
	<rets>
		<ret name="locked" type="boolean">Returns `true` if all stringtables are currently locked.</ret>
	</rets>
</function>