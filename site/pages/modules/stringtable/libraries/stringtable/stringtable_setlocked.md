<function name="SetLocked" parent="stringtable" type="libraryfunc">
	<description>
		Locks or Unlocks all stringtables.
		<added version="0.8"></added>
	</description>
	<realm>Server</realm>
	<args>
		<arg name="lock" type="boolean" default="false">`true` to lock them, `false` to unlock.</ret>
	</args>
</function>