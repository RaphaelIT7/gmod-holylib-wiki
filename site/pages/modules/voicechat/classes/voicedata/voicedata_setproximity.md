<function name="SetProximity" parent="VoiceData" type="classfunc">
	<description>
		Sets if the VoiceData is in proximity.
	</description>
	<realm>Server</realm>
	<args>
		<arg name="plySlot" type="boolean" default="false">Set to `true` if the VoiceData is in proximity</arg>
	</args>
</function>