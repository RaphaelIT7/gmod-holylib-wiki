<function name="ShouldSkip" parent="physenv" type="libraryfunc">
	<description>
		Returns whether the current skip type (see <page>physenv.GetPhysSkipType</page>) tells the engine to skip the current impact/simulation step.
		<note>
			Only works on Linux32
		</note>
	</description>
	<realm>Server</realm>
	<rets>
		<ret name="" type="boolean"></ret>
	</rets>
</function>
