<function name="__tostring" parent="ICollisionQuery" type="classfunc">
	<description>
		Returns `ICollisionQuery NULL` if invalid.
		Else it returns `ICollisionQuery`.
	</description>
	<realm>Server</realm>
	<args>
	</args>
	<rets>
	</rets>
</function>